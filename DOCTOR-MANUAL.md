# 心理医生（xinliyisheng）操作手册与权限合理性评审

> 适用范围：心理健康小程序（xinyu）Django 后端 + uni-app 前端
> 评审日期：2026-08-27
> 评审对象：`main/Xinliyisheng_v.py`、`Yuyuezixun_v.py`、`Jiankangyujing_v.py`、`Xinqingriji_v.py`、`Xinlizhishi_v.py`、`Yonghu_v.py`、`Examrecord_v.py`、`templates/front/utils/menu.js` 及各页面

---

## 一、医生角色定位（先对齐口径）

心理医生在这套系统里应当是**专业服务提供者**，而不是系统管理员。它的业务闭环应该是：

```
接收风险预警 → 查看"我的患者"档案（日记 + 测评报告）→ 处理/登记预警 → 审核预约 → 给出专业回复 → 回访跟进
```

一句话原则：**医生只能接触"和自己有预约关系的患者"的数据，只能对自己职责范围内的业务做"专业动作"（审、复、处理），绝不允许碰系统配置、其他医生、用户账号、题库组卷等管理性操作。**

---

## 二、医生可以做/应该做的操作手册

### 2.1 账号与个人信息（自己）

| 操作 | 入口 | 接口 | 合理性 |
|---|---|---|---|
| 登录 / 登出 | 登录页 / 个人中心 | `xinliyisheng/login`、`logout` | ✅ 应有 |
| 注册（工号唯一） | 注册页 | `xinliyisheng/register` | ⚠️ 见 7.2，建议改为管理员录入或审核制 |
| 修改自己的密码 | 个人中心 | `xinliyisheng/updatePassword` | ✅ 应有 |
| 密保/邮箱找回密码 | 找回页 | `xinliyisheng/recovery`、`security` | ✅ 应有 |
| 查看自己的资料 / 头像 / 简介 | 个人中心 | `xinliyisheng/session`、`info` | ✅ 应有 |

### 2.2 患者管理（必须严格绑定"预约关系"）

| 操作 | 接口 | 当前实现 | 判定 |
|---|---|---|---|
| 查看"预约过我的患者"的**心情日记** | `xinqingriji/page`、`list`、`info`、`detail` | `_apply_diary_scope` 按 `yishenggonghao` 反查预约用户过滤，逐条 `_can_read_diary` 校验 | ✅ 正确，只读 |
| 写/删患者日记 | `xinqingriji/update`、`delete` | `_can_write_diary` 对医生返回 False | ✅ 正确禁止 |
| 查看"我的患者"的**健康预警** | `jiankangyujing/page`、`list` | `yonghuzhanghao__in` 预约关系过滤 | ✅ 正确 |
| 处理/登记预警（改处理状态） | `jiankangyujing/update`、`delete` | `_can_write_warning` 仅限预约过自己的患者 | ✅ 正确 |
| 查看"我的患者"的**测评报告** | `examrecord/result`、`groupby` | ❌ 医生被当作"普通用户"，只能看自己（医生自己没有报告） | ❌ **设计断点**，见 6.1 |
| 查看患者**心语AI/倾诉会话**（如开放） | — | 未对医生开放读取接口 | 🔶 可选 |

### 2.3 预约咨询（审核）

| 操作 | 接口 | 当前实现 | 判定 |
|---|---|---|---|
| 查看"预约我的"列表 | `yuyuezixun/page`、`list` | `_apply_owner_scope` 按 `yishenggonghao` 过滤 | ✅ 正确 |
| 单条审核（是否审核 + 审核回复） | `yuyuezixun/update` | 医生仅允许改 `sfsh`、`shhf` 两个字段 | ✅ 正确 |
| 批量审核 | `yuyuezixun/shBatch` | 逐条 `_can_access_record` 校验 | ✅ 正确 |
| 修改预约其他内容 | `yuyuezixun/update` | 医生被限制在 `{id,sfsh,shhf}` | ✅ 正确禁止 |

### 2.4 在线咨询（"找医生问诊"）

> 表 `doctoradvice` 是"症状→建议"的 AI 辅助诊断助手，接口在 `Doctoradvice_v.py`。注意：**这是给"用户输入症状、AI+规则给建议"用的功能，不是医生的操作能力**，操作手册里医生侧暂不涉及。（见 8.1 更名的锅在于命名误导。）

### 2.5 预警接收（被动）

| 事件 | 接收人 | 实现位置 | 判定 |
|---|---|---|---|
| 心情日记风险预警 | 管理员 + "该患者预约过的医生" | `Xinqingriji_v._diary_warning_recipients` | ✅ 精准 |
| SCL-90 测评预警 | 管理员 + **全体医生** | `Examrecord_v._scl90_warning_recipients` | ⚠️ 广播，见 6.1 |
| 心语AI 危机预警 | 管理员 + **全体医生** | `xinyuai_v` recipients | ⚠️ 广播，同上 |

---

## 三、当前授权矩阵速查

| 资源 | 管理员(users) | 医生(xinliyisheng) | 普通用户(yonghu) |
|---|---|---|---|
| 患者日记 读 | 全部 | 仅预约过的患者 | 仅自己 |
| 患者日记 写/删 | 全部 | ✗ | 仅自己 |
| 健康预警 读 | 全部 | 仅预约过的患者 | 仅自己 |
| 健康预警 写/删 | 全部 | 仅预约过的患者 | ✗ |
| 测评报告 读 | 全部 | ❌（缺口） | 仅自己 |
| 预约咨询 读/审 | 全部 | 仅预约自己的 | 仅自己的记录 |

---

## 四、医生"不该管"的东西（当前全部放开了，属严重越权）

对比 `menu.js` 心理医生角色的 backMenu 与后端实际守卫，以下项**应该从医生后台菜单移除**，后端也要补守卫：

### 4.1 🔴 P0 — 用户账号可直接删除/修改（无后端守卫）

- 医生菜单里有「用户管理 → 新增/查看/删除」。
- 实测 `main/Yonghu_v.py` 的 `yonghu_delete`、`yonghu_update`、`yonghu_add` **完全没有 tablename 守卫**。即：拿着医生 token 调 `yonghu/delete` 可以直接删任何普通用户；调 `yonghu/update` 可以改任意用户字段（含 `mima` 密码列）。
- **这是数据安全红线，必须第一时间封堵**，见 7.1。

### 4.2 🔴 P0 — 其他医生账号可被增删改

- 医生菜单里有「心理医生管理 → 新增/查看/删除」。
- `Xinliyisheng_v.py` 的 `xinliyisheng_save/add/update/delete` 无管理员守卫，医生可以新增/删除其他医生、改其他医生资料（含重置密码？——`resetPass` 有 users 守卫，但 `update` 无守卫，仍可改别人 mima）。
- 医生之间应是平级关系，**绝不允许互删互改**。

### 4.3 🔴 P0 — 测评/题库/试卷管理

- 医生菜单里有「心理测试(组卷) exampaper」「心理测试试题 examquestion」的**新增/修改/删除/组卷**按钮。
- 题库、试卷、组卷是**平台内容管理**职责，与医生临床职责无关。后端 `exampaper_v` / `examquestion_v` 若无管理员守卫，医生可篡改测评题目与分值——直接影响 SCL-90 预警的公平性。
- 应整体从医生菜单移除，仅保留"查看"视产品需要。

### 4.4 🔴 P0 — 弹窗提醒 / 轮播图 / 心理资讯 / 心理知识（内容管理）

- 医生菜单里有「弹窗提醒管理」「系统管理/轮播图(config)」「心灵资讯 news」「心理知识 xinlizhishi」的新增/修改/删除按钮。
- 这些是平台运营内容，医生不应编辑。其中 `xinlizhishi/page` 后端还特意按 `yishenggonghao` 给医生做了数据隔离——说明产品本意是"医生只能看自己的知识投稿"，但菜单却给了删改所有人的按钮，前后矛盾。

### 4.5 🟡 P1 — 预约时段 `yuyueshiduan` ✅ 已修复（2026-08-27）

- 医生菜单有「预约时段管理 → 新增/修改/删除」。
- **修复结论**：`yuyueshiduan` 是全局时段字典（无医生归属列），不属任何单一医生，故定为「仅管理员可维护」。`main/Yuyueshiduan_v.py` 的 `save/add/update/delete` 四接口已补 `_is_admin_request` 守卫，非 `users` 一律 403「只有管理员才能维护预约时段」；医生/用户仅保留查看。

---

## 五、医生"目前缺失、应该增加"的功能

### 5.1 医生工作台（首页聚合）— 强烈建议
现在医生的全部操作散在十几个菜单项里，且和"内容管理"混在一起。建议做一个统一工作台：
- 待审核预约数、未处理预警数
- 我的患者列表（来自预约关系）
- 最近预警时间线

### 5.2 患者档案页（把日记+报告+预警串起来）
现在日记、报告、预警是三个独立入口，医生看同一个患者要跳三处。建议按"患者"维度聚合。

### 5.3 预警"处理"闭环
`jiankangyujing` 只有 `chulizhuangtai`（未处理/已处理）一个字段，没有**处理意见、处理人、处理时间、回访记录**。竞赛评审会问："医生收到预警后做了什么？"建议加：
- 处理备注（chulibeizhu）
- 处理人/处理时间
- 把医生的处理动作与审核回复打通

### 5.4 医生→患者的专业回复通道（可选）
现在医生能看日记，但没有任何"回复/关心"途径，只能靠预约审核的 `shhf` 间接沟通。可考虑在日记/预警上增加医生的"专业建议"回写（只读患者数据 + 医生可写回一条专业回复，患者可见）。

---

## 六、评审发现的核心设计断点

### 6.1 测评报告"推送全体医生 vs 医生无权限查看"（一致性冲突）

- 触发侧：`_scl90_warning_recipients` 把 SCL-90 预警推给**每一位医生**；心语AI危机预警同样广播全体医生。
- 读取侧：`examrecord_result` / `examrecord_groupby` 中，医生非管理员 → 退回 `userid = 自己`，医生自己没有测评 → 永远空、且显式返回"无权查看他人的测评报告"。

**后果**：医生收到"某用户 SCL-90 总分 280、危机"的预警弹窗，却打不开患者的报告，只能干瞪眼。这是评委最容易抓到的一处逻辑打架。

**修复二选一（推荐 A）**：
- **A**：医生读报告权限 = "该患者预约过我"（复用 `_diary_warning_recipients` 的预约关系判定），非预约患者维持 403。
- B：SCL-90 预警改为只推给"该患者预约过的医生"（与日记预警口径统一）。

### 6.2 前端给医生的"新增预警"按钮与实际权限不符

`jiankangyujing/list.vue` 里医生端菜单 `buttons` 含「新增」，前端会出现"新增预警"按钮；但后端 `_can_write_warning` 对医生写非预约患者返回 False，医生点新增必 403。应移除此按钮。

### 6.3 医生能"看到自己录入信息"的隔离与全局管理混用

`xinlizhishi/page` 对医生按 `yishenggonghao` 隔离，但菜单给了"首页总数/统计/删除评论"等管理级能力，主体责任边界不清。要么把它做成"医生投稿模块"，要么彻底从医生菜单移除。

---

## 七、必须优先修复的安全项（按紧急度排序）

### 7.1 P0：给所有"用户表/医生表"写接口补 tablename 守卫

在 `Yonghu_v.py`、`Xinliyisheng_v.py` 的以下接口入口处，**非 `users`（或非本人且非管理员）直接 403**：

- `yonghu_delete`、`yonghu_update`、`yonghu_add`（当前完全裸奔）
- `xinliyisheng_save/add/update/delete`（当前裸奔）

最小改动是仿照 `Jiankangyujing_v._can_write_warning` 写一个 `_must_be(request, 'users')` 守卫函数，统一拦截。

### 7.2 P1：医生注册放开 vs 账号真实性问题

`xinliyisheng_register` 任何人可注册医生账号（只用唯一工号），无实名/无审核流程。在心理健康场景，"伪医生"是可造成真实伤害的。建议：
- 医生账号改由管理员后台新增（`save`），前台注册关闭或加 `sfsh` 审核；
- 模型 `__sfsh__` 当前为"否"，应改"是"并走审核。

### 7.3 P1：`resetPass` 只能管理员（已有守卫 ✅），但需确认 `update` 不能绕道改密码

`xinliyisheng_register` 的密码、`update` 的 mima 字段需名统一，避免"改资料顺带改密码"的绕过路径。

---

## 八、命名与职责澄清（评审视角会追问）

### 8.1 「doctor advice」命名误导

`pages/doctor`（"你最近有哪些不适？症状→科室建议"）实际是**面向普通用户的自助问诊工具**，不是医生的操作。`doctoradvice` 表名、`Doctoradvice_v.py`、`pages/doctor` 目录名容易被评委误读为"医生助手模块"。
- 若保留，建议文档里明确它是"用户症状自查"功能；
- 若要严谨，改名 `symptom advice` 更贴合。

### 8.2 `examrecord` 的暗坑

- `examrecord_default`、`examrecord_query`、`examrecord_info/detail`、`examrecord_update/delete/deleterecords/vote` **都没有权限守卫**（`info/detail` 直接 `getbyid` 返回，`delete` 直接删）。医生 token 可以顺着这些接口读任意用户逐题作答明细、删测评记录。
- 这是继 4.1 之后的又一越权面，应和 7.1 一起封堵。

---

## 九、整改优先级总表

| 优先级 | 事项 | 类型 | 动作 |
|---|---|---|---|
| 🔴 P0 | `yonghu_delete/update/add` 无守卫 | 安全漏洞 | 补 `users` 守卫 |
| 🔴 P0 | `xinliyisheng_save/add/update/delete` 无守卫 | 安全漏洞 | 补守卫 |
| 🔴 P0 | `examrecord_info/detail/query/delete/update` 无守卫 | 安全漏洞 | 补守卫 |
| 🔴 P0 | 医生菜单出现：用户/医生/题库/试卷/弹窗/轮播图/资讯/知识管理 | 越权 | 移除菜单 + 补守卫 |
| 🔴 P0 | 测评报告：预警广播全体医生 vs 医生无读权限 | 设计断点 | 按预约关系授权读报告 |
| 🟡 P1 | 医生注册无审核 | 资质风险 | 关前台注册/加审核 |
| 🟡 P1 | 预约时段可改他人 | 越权 | ✅ 已修：仅管理员可维护，医生只读 |
| 🟡 P1 | 预警缺处理闭环字段 | 功能缺失 | 加回访/处理意见 |
| 🟢 P2 | 医生工作台/患者档案聚合 | 体验 | 新建页面 |
| 🟢 P2 | "doctor advice" 命名澄清 | 文档 | 说明或改名 |

---

## 附：医生应具备的操作清单（终稿）

**可以且应该做的：**
1. 登录/登出、改自己密码、找回密码、维护本人资料
2. 查看"预约过自己"患者的日记（只读）
3. 查看、处理"预约过自己"患者的健康预警
4. 查看、审核"预约自己"的预约咨询（改审核状态 + 回复）
5. 查看"预约过自己"患者的测评报告（**目前缺失，待补**）
6. 接收自己患者的风险预警

**不该做的（应移除/禁止）：**
- 增删改用户账号、改用户密码
- 增删改其他医生账号
- 管理测评试卷/试题/组卷
- 管理系统轮播图、弹窗提醒
- 编辑他人心理知识、资讯（自己投稿除外，需隔离）
- 修改预约时段（全局时段字典，仅管理员维护）
